# Remark template for copy&paste

Based on https://github.com/garbados/generator-remark

Workflow:
* `git checkout master` -- it's template branch
* `git checkout -b new-branch`
* do slides
* commit & push
